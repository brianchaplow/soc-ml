# =============================================================================
# Target VM Network Configuration
# Copy to .env and adjust for your environment
# =============================================================================

# Parent interface on the Docker host that has VLAN 40 access
# This is the interface connected to (or trunking) VLAN 40
# Examples: eth0, ens18, eth0.40 (if using VLAN subinterface)
PARENT_IFACE=eth0

# VLAN 40 subnet
SUBNET=10.10.40.0/24
GATEWAY=10.10.40.1

# Target IPs (must be unused on VLAN 40)
WORDPRESS_IP=10.10.40.30
CRAPI_IP=10.10.40.31
SERVICES_IP=10.10.40.32
HONEYPOT_IP=10.10.40.33

# WordPress credentials
WP_DB_PASSWORD=vulnerable123
WP_ADMIN_USER=admin
WP_ADMIN_PASS=admin

# crAPI
CRAPI_MAIL_PORT=8025

# Vulnerable services
FTP_USER=ftpuser
FTP_PASS=ftppass123
